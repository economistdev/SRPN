'''
You can copy your code into the .java files in the SRPN folder.
You might want to work on your code in this replit.
If you want to run your code  (without the test script) then you can compile and run your code using the Shell prompt on the right:
cd SRPN (changes the current directory to the SRPN folder)
javac *.java (compiles)
java Main (runs)
'''
import os

os.system("python mark-code.py")